package util;

/**
 * Observes stuff? I haven't really looked into how this is being used, whoever
 * added this should document it (i.e. I am too lazy to do it.) 
 */

public interface Observer {
	public void update(Subject s);
}
